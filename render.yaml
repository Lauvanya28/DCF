services:
  - type: web
    name: dialogflow-chatgpt-flask
    env: python
    buildCommand: ""
    startCommand: gunicorn app:app
    envVars:
      - key: OPENAI_API_KEY
        sync: false
